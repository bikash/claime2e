# Flyway config. Credentials come from FLYWAY_URL / FLYWAY_USER / FLYWAY_PASSWORD,
# which start.sh exports from .env — nothing secret lives in this file.
flyway.locations=filesystem:db/migration
flyway.cleanDisabled=true
flyway.validateMigrationNaming=true
flyway.outOfOrder=false
